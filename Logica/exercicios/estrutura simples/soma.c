#include <stdio.h>

int main()
{
    int x, y, s;

    printf("Digite o primeiro valor: ");
    scanf("%d", &x);
    printf("Digite o segundo valor: ");
    scanf("%d", &y);

    s=x+y;
    printf("SOMA: %d", s);
    return 0;
}
